const { expect } = require('chai');
const sinon = require('sinon');
const rewire = require('rewire');
const handler = require('../../src/handlers');
const diceRollCommandHandlers = require('../../src/handlers/dice-roll-command-handler');
const gameCreateCommandHandler = require('../../src/handlers/game-create-command-handler');


describe('Handler', function() {
    it('Should throw exception if it is invalid command', function() {

    })
})
